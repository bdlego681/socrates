import { Component } from '@angular/core'; import { Router, RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router'; import { AuthService } from '../core/auth.service';
@Component({ standalone: true, imports: [RouterLink, RouterLinkActive, RouterOutlet], template: `<div class="shell"><aside><a routerLink="/app/dashboard" class="portal-mark">P<span>Personal Portal</span></a><nav><a routerLink="/app/dashboard" routerLinkActive="active">Dashboard</a><a routerLink="/app/settings" routerLinkActive="active">Settings</a></nav><button class="logout" (click)="logout()">Log out <span>→</span></button></aside><section class="workspace"><header><span class="status-dot"></span><span>Private workspace</span></header><main class="content"><router-outlet /></main></section></div>` })
export class AppShellComponent { constructor(private auth:AuthService, private router:Router) {} logout(){ this.auth.logout().subscribe({next:()=>this.router.navigateByUrl('/login'), error:()=>{this.auth.user.set(null);this.router.navigateByUrl('/login')}}); } }
