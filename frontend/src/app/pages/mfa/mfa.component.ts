import { Component, signal } from '@angular/core'; import { FormControl, ReactiveFormsModule, Validators } from '@angular/forms'; import { Router, RouterLink } from '@angular/router'; import { AuthService } from '../../core/auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule,RouterLink],template:`<main class="public-page"><section class="auth-card"><a routerLink="/login" class="brand">← Back to login</a><h1>Verify your identity</h1><p class="muted">Enter the code from your authenticator app.</p><form (ngSubmit)="submit()"><label>{{recovery()?'Recovery code':'6-digit code'}}<input [formControl]="code" autocomplete="one-time-code" [placeholder]="recovery()?'XXXXXXXX-XXXXXXXX':'123456'" autofocus /></label>@if(error()){<p class="form-error">{{error()}}</p>}<button class="button wide" [disabled]="code.invalid||loading()">{{loading()?'Verifying…':'Verify'}}</button></form><button class="link-button" (click)="recovery.set(!recovery())">{{recovery()?'Use authenticator code':'Use a recovery code'}}</button></section></main>`})
export class MfaComponent { code=new FormControl('',{nonNullable:true,validators:[Validators.required]}); recovery=signal(false); loading=signal(false); error=signal(''); constructor(private auth:AuthService,private router:Router){} submit(){this.loading.set(true);this.error.set('');this.auth.verifyMfa(this.code.value,this.recovery()).subscribe({next:()=>this.router.navigateByUrl('/app/dashboard'),error:()=>{this.error.set('Unable to verify identity.');this.loading.set(false)}})} }
